# Python / virtualenv / build artifacts
.venv/
__pycache__/
*.py[co]
*.pdf

# Claude local (machine-specific) settings
.claude/settings.local.json

# ALL real/personal content lives under data/ — never committed.
# This is the master CV (data/cv.md), job criteria (data/search-profile.md),
# style-reference CVs (data/references/), the database, scraped JDs, tailored
# CVs, and per-run digests. Copy the templates/ into data/ to get started:
#   cp templates/search-profile.example.md data/search-profile.md
#   cp templates/cv.example.md             data/cv.md
/data/
